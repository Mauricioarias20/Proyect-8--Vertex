{
  "name": "portfolio-killer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm --prefix backend install --include=dev && npm --prefix frontend install",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend run build"
  },
  "engines": {
    "node": "18.x"
  }
}
